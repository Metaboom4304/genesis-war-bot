services:
  - type: web
    name: genesis-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TELEGRAM_TOKEN
        fromEnv: TELEGRAM_TOKEN
